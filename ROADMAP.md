# City-Sim Roadmap

このファイルは、City-Simで今後実装を検討・予定している主要項目を公開して管理するためのロードマップです。

ここに書かれた内容は将来の実装を保証するものではなく、優先度・対象バージョン・内容は開発状況に応じて変更される場合があります。現行の実装仕様はソースコードと`doc/`配下の現行ドキュメントを正とします。

## 管理方針

- `ROADMAP.md`では、中長期の機能追加・大きな改善・まとまりのある開発テーマを管理します。
- 個別の不具合、細かな修正、単発の作業は原則としてGitHub Issuesで管理します。
- 実装を開始した項目は、必要に応じてIssueまたはPull Requestを作成し、このファイルから参照します。
- ロードマップの項目は、状態・優先度・対象・完了条件が分かるように記載します。
- 実装予定が未確定の場合、無理に対象バージョンや期限を設定しません。未確定値は`TBD`とします。
- 日付やリリース時期は、実際に確定した場合のみ記載します。
- 項目の追加・削除・優先度変更・延期は、判断理由が分かるようにNotesへ簡潔に残します。
- 実装が完了した項目は`Done`へ移し、関連するPR・Issue・Releaseがある場合はリンクします。
- 中止した項目は削除せず、`Dropped`へ変更して理由を残します。単なる重複項目は統合して構いません。
- ロードマップだけを更新する変更も、通常のcommit / Pull Request / version運用に従います。

## Status

各項目の`Status`には次のいずれかを使用します。

| Status | 意味 |
| --- | --- |
| `Planned` | 実装予定。まだ着手していない |
| `In Progress` | 実装または具体的な設計に着手している |
| `Blocked` | 依存関係や未解決事項により進行できない |
| `Deferred` | 実装予定は維持するが、当面は着手しない |
| `Done` | 実装・必要な検証が完了した |
| `Dropped` | 実装しないことを決定した |

## Priority

優先度は必要な場合のみ設定します。

| Priority | 意味 |
| --- | --- |
| `High` | 次期開発で優先して扱う |
| `Medium` | 通常の実装候補 |
| `Low` | 長期候補、または余裕がある場合に扱う |
| `TBD` | 優先度未決定 |

優先度は重要度・緊急度・依存関係・実装コストなどを総合して変更できます。並び順だけを優先度の根拠にしません。

## 項目フォーマット

新しい項目は原則として次の形式で追加します。

```markdown
### <実装テーマ名>

- Status: `Planned`
- Priority: `TBD`
- Target: `TBD`
- Summary: <何を実現するかを1〜3文で記載>
- Scope:
  - <主な対象領域>
  - <主な対象領域>
- Dependencies:
  - <前提となる実装・判断・Issue。なければ`None`>
- Completion Criteria:
  - <完了と判断できる条件>
  - <必要な検証条件>
- Tracking:
  - Issue: <URLまたは`TBD`>
  - PR: <URLまたは`TBD`>
- Notes:
  - <補足、判断理由、変更履歴など。なければ`None`>
```

### 記載ルール

- タイトルは実装手段ではなく、利用者またはシミュレーション上の目的が分かる名前を優先します。
- `Summary`は詳細設計書の代わりにせず、目的と到達点だけを簡潔に書きます。
- `Scope`には関係する主要領域だけを書き、細かなサブタスクを列挙しすぎません。
- `Dependencies`には、その項目を始める前に必要な実装・判断・外部条件を記載します。
- `Completion Criteria`は「何を実装したか」ではなく「どうなれば完了か」を確認できる形にします。
- `Tracking`は実作業を追跡するIssue / PRへの入口として使います。詳細な進捗ログをROADMAPへ書き続けません。
- `Notes`には、延期・中止・方針転換など、後から見て理由が必要になる判断を残します。

## Roadmap Entries

### 都市診断オーバーレイ

- Status: `Planned`
- Priority: `High`
- Target: `TBD`
- Summary: 都市全体の状態を3Dビュー上で即座に診断できるオーバーレイ機能を追加します。まず共通基盤と交通・電力オーバーレイを実装し、人口・公共交通・雇用・土地利用・物流・アクセシビリティ・開発者診断へ拡張できる構造にします。
- Scope:
  - 共通`OverlayManager`とオーバーレイ選択UI。通常表示と独立して有効/無効を切り替え、表示濃度を調整できるようにする。
  - 交通: 道路の混雑度、交通量、平均速度/制限速度比、信号・交差点待ち時間を道路上の半透明帯などで可視化する。
  - 電力: 建物の給電状態、変電所負荷率、Zone、電圧状態、発電・外部受電設備の稼働状態を可視化する。通常時は非表示の地下送電経路を、電力オーバーレイ時のみ診断線として表示できるようにする。
  - 公共交通: バス・鉄道路線、停留所/駅の待ち人数・利用者数・混雑率・徒歩圏を可視化する。
  - 人口: 人口密度、昼間人口/夜間人口、現在の人の集中と時間帯による移動をヒートマップ表示する。
  - 雇用: 職場定員充足率、現在出勤率、人手不足、ライフライン職員不足、通勤流動を可視化する。
  - 土地利用・建物稼働: 住宅、商業、オフィス、工業、物流、公共施設、電力施設などの用途とPOI利用率を可視化する。
  - 物流: 工場→倉庫→店舗、火力発電所向け燃料物流、在庫不足、トラック積載率、物流ボトルネックを可視化する。
  - アクセシビリティ: 駅・職場・商業・公共施設などへの到達時間やアクセス性を可視化する。
  - 開発者診断: Rendering LOD、チャンク、Agent/Vehicle密度、Pathfinding負荷、Simulation update負荷などを確認できる診断オーバーレイを用意する。
  - 表現は原則として、道路は色付き半透明帯、建物は軽い着色または地面ヒートマップ、施設は輪郭/アイコン強調を使い、通常の3D景観を完全に塗り潰さない。
  - 画面の過密化を避けるため、基本は「ヒートマップ系1レイヤー + ライン/アイコン系1レイヤー」程度を同時表示上限の目安とする。
- Dependencies:
  - 現行の交通、電力、公共交通、人口、雇用、物流システムから診断用Snapshotを低コストで取得できること。
  - オーバーレイ表示による描画・シミュレーション負荷の許容範囲を定義すること。
- Completion Criteria:
  - 共通オーバーレイ基盤から表示モードを切り替えられ、オーバーレイOFF時に通常描画やシミュレーション結果を変化させない。
  - 第1段階として交通と電力の主要指標を3Dビュー上でリアルタイムに確認できる。
  - シミュレーション時間を進めたとき、混雑・需要・人口集中など時間変化する指標が継続的に更新される。
  - 電力オーバーレイ以外では電線・電柱・送電経路を表示しない。電力オーバーレイでも通常景観用の架空電線としてではなく、診断用レイヤーとして扱う。
  - 大都市規模でもオーバーレイ有効時の追加負荷が性能目標内に収まり、既存LOD・Instanced描画の方針を崩さない。
- Tracking:
  - Issue: `TBD`
  - PR: `TBD`
- Notes:
  - 初期実装は「共通基盤 + 交通 + 電力」を優先し、その後に公共交通・人口・雇用・土地利用・物流・アクセシビリティ・開発者診断を追加する。
  - オーバーレイUIは専用ショートカット（例: F9）を候補とするが、既存キー割り当てと操作性を確認して実装時に確定する。
