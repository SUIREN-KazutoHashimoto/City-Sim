# Realistic City Simulation — アーキテクチャ設計書

three.js + TypeScript による大規模リアルタイム・シティシミュレーションの基盤。
挙動のリアルさ最優先。将来的な使い回し・拡張のために、明確な層分割と一貫した
インターフェースを持つ。

---

## 0. 最重要の設計判断:OOP と 60fps の両立

> 「大量属性・継承・カプセル化のOOP」と「数十万エージェントで60fps」は
> **素朴に実装すると両立しません。** これを最初に解決しておく必要があります。

各エージェントを `class Pedestrian { pos; needs; update() }` のような
「1体=1オブジェクト」で持ち、毎フレーム `for (a of agents) a.update()` を回すと:

- オブジェクトがヒープに散らばり **キャッシュミス** が多発
- 仮想関数呼び出しのオーバーヘッド
- GC 圧迫

…で、数万体すら 60fps を割ります。

### 解決策:2層アーキテクチャ

| 層 | 役割 | 実装 | ファイル |
|----|------|------|----------|
| **OOP ドメイン層(ファサード)** | 拡張性・可読性・API。継承・カプセル化・属性はここ | `GameObject` 階層。**配列の1行を指す薄いハンドル** | `world/GameObject.ts`, `agents/Pedestrian.ts` |
| **データ指向 実行層 (SoA)** | 性能。全エージェント状態を列ごとの TypedArray に | `AgentStore`。System がまとめて線形走査 | `agents/AgentStore.ts` |

- **ハンドルは大量生成しない。** 選択中/検査対象のオブジェクトだけ包む。
- SoA なら `SharedArrayBuffer` に載せて **Web Worker と共有**でき、
  `InstancedMesh` バッファへも**一括コピー**できる(描画も速い)。

これで「表現力のOOP」と「キャッシュ効率の実行」を同時に満たします。

---

## 1. 層構成

```
main.ts  ── three.js セットアップ + 固定ステップループ
  │
  ├─ World (world/World.ts) ── 全システムのオーケストレータ(配線と実行順序のみ)
  │    ├─ SimulationClock       固定ステップ時間源 / ゲーム内カレンダー
  │    ├─ NeedSystem            ニーズ減衰
  │    ├─ UtilityBrain          効用ベース意思決定(目的選定→目的地検索)
  │    ├─ AStar / RoadNetwork   交通網・経路探索
  │    ├─ SpatialHashGrid       近接クエリ(回避・LOD判定)
  │    ├─ AgentStore (SoA)      全エージェント状態
  │    └─ POIRegistry           目的地(用途を持つ場所)
  │
  ├─ CityGenerator (generation/) ── 手続き型都市生成(地形→地区→道路→区画→建物/POI)
  │
  └─ InstancedRenderer (rendering/) ── GPUインスタンシングによる描画
```

**依存方向は一方向**:実行層(SoA/System)は three.js に依存しない
→ ヘッドレスでテスト・ワーカー実行が可能(実際に headless で検証済み)。

---

## 2. 意味的な歩行者AI(Utility AI)

要件「各自のステータスから目的を出し、目的地を検索して移動する」の実装。

```
ニーズ減衰(NeedSystem)
   → 各行動の効用スコア算出(UtilityBrain: energy低→"sleep", hunger低→"eat" …)
   → 最大効用の行動を選択
   → その用途カテゴリのPOIを近傍検索(POIRegistry.findBest: 距離×価格帯)
   → state=Routing → 経路探索 → Traveling(経路追従)
   → 到達 → Engaged(活動でニーズ回復)→ 満了 → Idle
```

- **拡張は宣言的**:`UtilityBrain.ACTIONS` に1エントリ足すだけで新しい欲求/行動が増える(開放閉鎖原則)。
- 時刻依存(9-18時は仕事、22-6時は睡眠バイアス)を効用に織り込み済み。
- 職業・年齢・所持金(wealth)を意思決定パラメータとして保持。

---

## 3. 交通網

- 道路 = **有向グラフ**(交差点ノード / 道路エッジ)。道路種別・車線・制限速度を保持。
- 経路探索 = **バイナリヒープ付き A***(所要時間コスト)。DOM非依存でワーカー実行可能。
- 車両追従 = **IDM(Intelligent Driver Model)** を導入予定(前方車間で加減速、渋滞が自然発生)。
  ※本スケルトンでは歩行者ステアリングまで実装。車両は `Vehicle` 用 Store を追加して同型で実装する。
- 信号 = ノードの `hasSignal` フラグ。フェーズ制御を `TrafficController` として追加。

### 大規模化(150km²)での経路探索
交差点が数万〜十万規模になったら **階層型経路探索(HPA*)** へ:
地区を高次ノードに畳んで粗く探索 → 局所で詳細化。経路探索は **Web Worker** に隔離。

---

## 4. 手続き型生成

1. **都市化度マップ**(value-noise fbm + 中心バイアス)を生成
2. 目標市街地比(初期=1/3)になるよう**分位点から閾値を自動キャリブレーション**
3. 閾値以上のセルに密な道路格子(郊外は幹線のみ間引き)
4. 街区を区画分割 → 建物配置。都市化度で**用途分布と階数**を変える(中心=高層/商業、周縁=住宅)
5. 建物に**POIを登録**(1棟に複数用途が入りうる)

決定論的(同一シードで同じ街)。生成時間はマップ規模に対して線形で、大規模でも許容範囲。

---

## 5. 性能戦略(Ryzen 7 7800X3D で平均60/最低25fps 目標)

| 手法 | 目的 | 状態 |
|------|------|------|
| SoA(TypedArray)+ System 線形走査 | キャッシュ効率 | ✅ 実装済 |
| **固定ステップ + 可変描画** | 挙動の決定性と安定FPS | ✅ 実装済 |
| **空間ハッシュ**で近接クエリ | O(1)近傍・回避 | ✅ 実装済 |
| **GPUインスタンシング** | 建物/エージェントを1ドローへ | ✅ 実装済 |
| **意思決定の時間分割**(1フレーム512体上限) | AIコスト平準化 | ✅ 実装済 |
| **シミュレーションLOD**(遠方は低頻度更新/統計化) | 遠方エージェントの計算削減 | ⏳ 次段階 |
| **チャンク・ストリーミング**(タイル単位で生成/破棄) | 150km²のメモリ/描画 | ⏳ 次段階 |
| **Web Worker + SharedArrayBuffer** | 経路探索・生成を別スレッドへ | ⏳ 次段階 |
| **インポスター/ビルボード**(遠景建物) | 遠方描画コスト | ⏳ 次段階 |
| **WASM**(ホットループのSIMD化) | 究極の最適化 | 🔮 将来 |

> **150km²を全て毎フレーム完全シミュレーションするのは不可能。**
> カメラ周辺の「アクティブバブル」のみ完全シミュレーション、遠方は
> **シミュレーションLOD**(集計的近似)にするのが必須。SoA設計はこのLOD化に最適。

### 検証済みの初期性能(headless, 単スレッド naive)
- 生成 86ms(建物1790 / 道路ノード524 / POI1971)
- 4000エージェントで **約4.08 ms/step**(空間ハッシュ再構築+ステアリング込み)
- → まだ最適化前。時間分割・LOD・ワーカー化で1桁改善の余地。

---

## 6. 開発ロードマップ

### Phase 0 — 基盤(現在) ✅
10km² / 市街地1/3。生成・意味的歩行AI・GPU描画・固定ステップループが動く土台。

### Phase 1 — 交通の実装
`Vehicle` Store + IDM追従 + 交差点/信号制御。歩行者を歩道グラフに乗せる。

### Phase 2 — シミュレーションLOD & ストリーミング
アクティブバブル、遠方統計化、タイル単位のチャンク生成/破棄。規模を50→150km²へ拡張。

### Phase 3 — 並列化
経路探索・生成・ニーズ更新を Web Worker へ。SharedArrayBuffer でSoA共有。

### Phase 4 — グラフィック品質
用途別テクスチャアトラス、窓ノーマル/夜景エミッシブ、街路樹・信号・車両モデル、
時間帯ライティング、遠景インポスター。「ハリボテ」感の除去。

### Phase 5 — 深い意味シミュレーション
経済(雇用・賃金・物価)、世帯・家族、公共交通、需要に応じた都市の動的変化。

---

## 7. 起動方法

```bash
npm install
npm run dev      # http://localhost:5173
```

操作:ドラッグ=回転 / ホイール=ズーム。左上HUDに時刻・統計・FPS。

---

## 8. 拡張の指針(OOP契約)

- 新しい動的オブジェクト種別 → `GameObject` を継承し `kind` と対応 Store を用意。
- 新しい欲求/行動 → `UtilityBrain.ACTIONS` にスコア関数を追加。
- 新しい用途 → `POICategory` に追加し `CityGenerator.pickCategory` に分布を定義。
- 新しいシステム → `World.step()` の実行順序に配線(System は単一責任を維持)。
