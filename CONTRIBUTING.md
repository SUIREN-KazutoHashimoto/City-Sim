# Contributing to Machi-Sim

Machi-Simへの貢献に関心を持っていただきありがとうございます。

## 開発の基本方針

- 通常の開発は最新の`develop`を基準にしてください。
- `main`はリリース系統として扱い、通常開発では直接更新しません。
- 変更は目的ごとに小さくまとめ、既存挙動を維持する部分と意図的に変更する部分を明確にしてください。
- 現行ソースコードを最優先の仕様とし、必要に応じて`doc/`配下の現行ドキュメントを参照してください。

## ブランチ名

目安として次の形式を使用してください。

- `feat/<topic>`: 新機能
- `fix/<topic>`: 不具合修正
- `perf/<topic>`: 性能改善
- `docs/<topic>`: 文書や公開設定

## 開発環境

Node.jsとnpmを使用します。

```bash
npm ci
npm run dev
```

変更後は最低限、次を実行してください。

```bash
npm run build
npm run bench:power
```

`npm run build`ではTypeScript、Vite production build、および配布用ライセンスファイルの生成を確認します。

## バージョン

このリポジトリでは、コミットを作成するたびに`src/version.ts`の`APP_VERSION`を更新します。バージョンだけを後追いの別コミットで上げず、実際の変更と同じコミットへ含めてください。

## Pull Request

Pull Requestは原則として`develop`を対象にしてください。PRには次を含めてください。

- 変更の目的
- 主な変更点
- 実施した検証
- UIや描画変更がある場合は、必要に応じてスクリーンショットや動画
- 未確認事項や既知の制約がある場合はその内容

CIが成功していても、描画、交通、鉄道、エージェント挙動などはブラウザ上での実機確認が必要になる場合があります。

## Issue

不具合報告では、可能な範囲で再現条件、期待する挙動、実際の挙動、使用バージョン、ブラウザ、seedや設定値、画像・動画を添えてください。

新機能提案では、解決したい問題、期待する挙動、代替案があれば記載してください。

## ライセンス

貢献として提出されたコードや文書は、リポジトリの`LICENSE`に記載されたApache License 2.0の下で配布されることに同意したものとして扱います。

行動規範については`CODE_OF_CONDUCT.md`、脆弱性報告については`SECURITY.md`を参照してください。
